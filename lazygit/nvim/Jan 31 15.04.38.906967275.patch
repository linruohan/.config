--- a/core/general_config.vim
+++ b/core/general_config.vim
@@ -56,13 +56,14 @@ if has('wildmenu')
 	" if has('nvim')
 	"		set wildoptions=pum
 	" else
+	"		set nowildmenu
 	if has('nvim')
 	      	set wildoptions=pum
 	else
 	      	set nowildmenu
 	      	set wildmode=list:longest,full
 	      	set wildoptions=tagfile
 	endif
 	set wildignorecase
 	set wildignore+=.git,.hg,.svn,.stversions,*.pyc,*.spl,*.o,*.out,*~,%*
 	set wildignore+=*.jpg,*.jpeg,*.png,*.gif,*.zip,**/tmp/**,*.DS_Store
